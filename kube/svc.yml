apiVersion: v1
kind: Service
metadata:
  name: data-service-svc
spec:
  selector:
    app: data-service-deployment
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080